# Gatekeeper Bot
BOT_TOKEN=
BOT_USERNAME=jrvisibut2

# MTProto (my.telegram.org)
API_ID=
API_HASH=

# Session encryption (Fernet key, generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# Database
DATABASE_URL=postgresql+asyncpg://autopilot:autopilot@localhost:5432/autopilot

# Redis (cache, queues, rate limiting)
REDIS_URL=redis://localhost:6379/0

# Backend auth
JWT_SECRET=
ADMIN_JWT_SECRET=
ADMIN_USERNAME=
# bcrypt hash, e.g. via: python -c "import bcrypt; print(bcrypt.hashpw(b'pass', bcrypt.gensalt()).decode())"
ADMIN_PASSWORD_HASH=

# Logging
LOG_LEVEL=INFO

# Gatekeeper bot
REQUIRED_CHANNELS=@your_channel
WEBAPP_URL=https://your-mini-app.example.com
